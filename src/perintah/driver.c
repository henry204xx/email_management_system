// #include <stdio.h>

// int main() 
// {
//     char* array[] = {"asd", "ASDA", "ASDFfa", "ASDASFA", "Anjayyy bisa cok"}; // Example array
//     int size = sizeof(array) / sizeof(array[0]); // Calculate the size of the array

//     for (char **ptr = array; ptr < array + size; ptr++) {
//         printf("Element: %s\n", *ptr);
//     }
//     return 0;
// }
